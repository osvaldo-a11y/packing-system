services:
  postgres:
    image: postgres:16-alpine
    container_name: packing-system-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: packing_system
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d packing_system"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
